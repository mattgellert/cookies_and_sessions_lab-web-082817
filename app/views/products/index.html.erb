<%= form_tag "products/add" do %>
  <%= text_field_tag :product %>
  <%= submit_tag "add to cart"%>
<% end %>

<table>
  <tbody>
      <% session[:cart].each do |product| %>
        <tr><%= product %></tr><br>
      <% end %>
  </tbody>
</table>
